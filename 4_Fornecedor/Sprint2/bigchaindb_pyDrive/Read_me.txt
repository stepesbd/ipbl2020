Instalar as seguintes bibliotecas para python 3:

sudo apt-get update

sudo apt-get install python3-dev libssl-dev libffi-dev

pip3 install bigchaindb_driver

Executar os scripts em python 3.

Versão 0:

- Primeira versão funcional do script.

To Do:

- Criar script para conexão com o mysql para consulta das chaves privadas e públicas dos fornecedores.
- Alterar o script para buscar os blocos que não foram transferidos, usando a rotina adequada da biblioteca.